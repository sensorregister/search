---

spring:
  application:
    name: search

feign:
  hystrix:
    enabled: false

---

spring:
  profiles: dev

eureka:
  client:
    enabled: false

register:
   ribbon:
      listOfServers: http://localhost:8081